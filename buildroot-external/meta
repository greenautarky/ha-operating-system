VERSION_MAJOR="16"
VERSION_MINOR="3"
VERSION_SUFFIX="1.9"

HASSOS_NAME="GreenAutarky BOS"
HASSOS_ID="haos"

DEPLOYMENT="production"

# The retired pre-2026-03-27 ("F13") signing CA bridge is GONE, not disabled.
# GA_LEGACY_CA_BRIDGE and ota/legacy-signing-cert.pem were both deleted on
# 2026-07-30: the remaining pre-cut devices are being swapped, not bridged, so
# re-trusting a non-revocable CA:TRUE root valid to 2035 has no purpose left.
# Bridge-FORWARD for old devices is unaffected — that signs a NEW image with the
# OLD KEY (archived off the builder, deliberately not destroyed) and never needed
# the retired cert in anyone's keyring.
