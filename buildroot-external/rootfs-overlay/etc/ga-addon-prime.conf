# BOSv1.2.21-rc1 addon prime list — addons that need a one-time
# `ha addons update` after the OS-OTA reboot.
#
# Specific to BOSv1.2.21-rc1: deliver ga_manager 0.44.1 (= fix for
# 0.44.0's `FileNotFoundError: 'ha'` bug in the self-update worker).
# Devices arriving on this OS still have 0.44.0 running; this prime
# is what gets them onto 0.44.1.
#
# After 0.44.1 is on every device, future ga_manager bumps no longer
# need this file (= devices will self-update via fleet-manager-driven
# `addon-update` job, which now correctly invokes
# `docker exec hassio_cli ha addons update <slug>`).
99f1cad4_ga_manager
