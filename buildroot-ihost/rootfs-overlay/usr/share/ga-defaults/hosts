# /etc/hosts - static hostname resolution
# Standard localhost entries
127.0.0.1       localhost
::1             localhost ip6-localhost ip6-loopback

# GreenAutarky service entries are managed by ga-update-hosts.service
# at boot, reading from /etc/ga-services.conf (or /mnt/data/ga-services.conf).
# Do NOT add GA IPs here — they will be overwritten.
