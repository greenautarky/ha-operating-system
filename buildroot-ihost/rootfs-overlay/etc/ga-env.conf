# GreenAutarky environment configuration
# Baked at build time — override at runtime via /mnt/data/ga-env.conf
#
# Values:
#   GA_ENV:        dev | prod
#   GA_LOG_LEVEL:  debug | info | warning
#   GA_TELEMETRY:  verbose | minimal | off

GA_ENV=dev
GA_LOG_LEVEL=debug
GA_TELEMETRY=verbose
