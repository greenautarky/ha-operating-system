[Unit]
# GA boot-time gate: bluetoothd only runs when the Bluetooth boot marker
# exists (default OFF, fleet-safe). See ga-bluetooth.service.
ConditionPathExists=/mnt/boot/ga-bluetooth
RequiresMountsFor=/mnt/boot
